import 'package:flutter/material.dart';

class LabelFormPage extends StatelessWidget {
  const LabelFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Formulário de Etiqueta'),
      ),
      body: Text('Nada aqui'),
    );
  }
}